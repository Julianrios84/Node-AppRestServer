const Server = require('./server.model');
const User = require('./user.model');
const Role = require('./role.model');
const Category = require('./category.model');
const Product = require('./product.model');

module.exports = {
  Server, User, Role, Category, Product
}